# Configurations

This directory will contain versioned model, training, and evaluation
configurations. A configuration associated with a reported experiment must not
be modified in place; create a new version instead.
